import { DISPLAY_ID } from "./constants";

const Details: React.FC = () => {
    return (
        <div id={DISPLAY_ID} className="h-full bg-black transition-width duration-500" style={{ width: "75%" }}>

        </div>
    )
}

export default Details;